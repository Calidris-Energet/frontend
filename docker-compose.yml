services:
  nginx-frontend:
    container_name: frontendapp
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8011:443
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    environment:
      NODE_ENV: production
